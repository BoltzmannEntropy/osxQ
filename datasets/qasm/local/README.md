This folder holds local OpenQASM circuits used for development and examples.

Preferred path for tests is `tests/data/qasm/minimal` with tiny fixtures. This
directory may contain larger samples not suitable for unit tests.

Search order in code resolves here first, then falls back to legacy `qasm_circuits/`.

